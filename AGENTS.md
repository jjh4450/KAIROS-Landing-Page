# Repo Overview

단일 git repo, 두 독립 프로젝트:

- `web/` — SvelteKit (pnpm)
- `backend/` — PocketBase (Go 바이너리 + JS hooks/migrations)

pnpm workspace는 쓰지 않습니다. 각 폴더 안에서 작업하세요.

상세 가이드:
- 프론트엔드: `web/AGENTS.md` (또는 `web/CLAUDE.md`)
- 백엔드: `backend/README.md`

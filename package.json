{
	"name": "kairos-monorepo",
	"private": true,
	"scripts": {
		"dev:web": "pnpm --filter ./web dev",
		"build:web": "pnpm --filter ./web build",
		"check:web": "pnpm --filter ./web check",
		"lint:web": "pnpm --filter ./web lint",
		"dev:backend": "node -e \"console.log('cd backend && ./pocketbase serve (Windows: pocketbase.exe serve)')\""
	}
}

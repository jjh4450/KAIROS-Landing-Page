# Settings-only pnpm file (no workspace packages).
# In pnpm v11+ this is the only place pnpm reads settings from —
# the "pnpm" field in package.json is no longer read.
# See: https://pnpm.io/settings

onlyBuiltDependencies:
  - '@tailwindcss/oxide'
  - esbuild
  - sharp
